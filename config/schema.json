{
    "env": {
      "doc": "The applicaton environment.",
      "format": ["production", "development", "integration"],
      "default": "development",
      "env": "NODE_ENV"
    },
    "database": {
      "doc": "The Database URL",
      "default": "",
      "env": "DATABASE_URL"
    }
}
